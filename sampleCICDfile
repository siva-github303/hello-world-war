pipeline {
    agent any
    tools {
        maven "localmaven"
    }
   

    stages {
        stage("Maven Build") {
            
            steps {
                script {
                    bat "mvn package -DskipTests=true"
                }
            }
        }
        }
        }
