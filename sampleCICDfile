pipeline {
    agent any
    environment { 
        CC = 'clang'
        java = 'test'
    }
    stages {
        stage('Example') {
            environment { 
                DEBUG_FLAGS = '-g'
            }
            steps {
                echo env.cc
                echo env.java
                echo env.debug_flags
            }
        }
    }
}
